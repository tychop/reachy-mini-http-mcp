{
  "name": "reachy-mini-http-mcp",
  "version": "1.0.0",
  "description": "MCP server for controlling Reachy Mini robot via HTTP API",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
  ,
  "devDependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  }
}
